apiVersion: harness.dev/v1
kind: Recipe
metadata:
  id: create-agent-instructions
  module: docs
when:
  finding: agents.instructions
operations:
  - type: file.create
    path: AGENTS.md
    content: |
      # Agent instructions

      Run the project verification command before delivery.
    ownership: seeded
