schema: 1
project:
  mode: new
  detected: {}
integration:
  policy: preserve-existing
  auto_fix_max_priority: P2
plugins:
  project-baseline: true
  spec-driven: true
commands:
  verify: "npm test"
policies:
  require_tests: true
  production_access: approval
